# npm 镜像源（npm 已知配置项）
registry=https://registry.npmmirror.com

# Electron 二进制镜像：不再写在本文件里。
# `electron_mirror` 是 npm 的**未知配置项**：npm 11 会告警「Unknown project config」，
# npm 12 起会直接报错，因此改用 @electron/get 官方支持的环境变量 ELECTRON_MIRROR。
# 需要走镜像时（国内网络），在安装前设置：
#   Windows PowerShell : $env:ELECTRON_MIRROR = "https://npmmirror.com/mirrors/electron/"
#   macOS / Linux      : export ELECTRON_MIRROR="https://npmmirror.com/mirrors/electron/"
# 然后重新执行 npm install 即可；网络通畅时无需设置。
