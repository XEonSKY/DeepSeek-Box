root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
indent_style = space
indent_size = 4

[*.md]
trim_trailing_whitespace = false

# YAML 缩进敏感（序列项的子键必须与 name 对齐而非再缩进一层），
# 且 GitHub Actions 生态惯例为 2 空格，故单独保留。
[*.{yml,yaml}]
indent_size = 2
